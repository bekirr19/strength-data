Accessible on/off toggle — accent track when on, spring knob. Used in profile settings.

```jsx
<Switch checked={on} onChange={setOn} ariaLabel="Enable reminders" />
```

Sizes `sm | md`. Pair with a label row yourself.
